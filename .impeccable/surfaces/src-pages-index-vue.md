---
version: 1
slug: "src-pages-index-vue"
primary_target: "src/pages/index.vue"
related_targets: ["src/pages/dash/executive.vue","src/pages/dash/sales.vue","src/pages/dash/products.vue","src/pages/dash/staff.vue","src/pages/dash/operations.vue"]
---

# Continuous dashboard

Mode: Operate. Scope: src/pages/index.vue and the five dashboard report sections. The original structural seed is 3d3e4b25. The existing blue/navy identity, six light/dark palettes, Hanken Grotesk and JetBrains Mono remain authoritative.

Approved direction, 2026-09-10: the user supplied .impeccable/mocks/dashboard-soft-charts-reference.png and explicitly authorized changing the header, cards and entire dashboard into that style. The former header/cards exception and comp A's visual styling are superseded. The built result uses soft raised surfaces, shaded rounded data marks, separated rounded donut sectors and floating value labels throughout. The reference's EV content is visual guidance only; restaurant metrics remain sourced from existing APIs. The dashboard is approved; further AI and Orders redesign work is outside this surface.

Composition: one continuous Overview, Sales, Products, Staff and Operations report. The renewed heading, independent Today's Orders card, custom date/time controls and sticky section navigation precede four tinted overview KPI cards. Performance and Order channels form the first equal-width chart pair; equally sized, large payment and category pies form the next row. Recent activity is a responsive grid of compact order tiles showing identity, amount, status, channel, table/address and age, with direct order links and access to every returned record. Later chapters also use separated tinted KPI cards, compact reports and truthful empty states. All fields, routes, permissions, filters, requests and actions remain available.

The Performance chart retains area/column, revenue/orders/AOV, supported comparison, point selection and optional exploration controls. Order channels is a simple three-column Hall/Delivery/Pickup comparison, with a custom selector for the entire supplied period or one reported date/bucket. It has exact counts and shares below the plot; missing counts retain dashes and an incomplete-data notice. It is not a flow diagram or a dense multi-series trend.

Products begins with two side-by-side interactive unit-share pies, one for sold products and one for sold categories. Each large pie pairs actual SVG ring sectors with five leading exact-value rows and a visible reported-dataset count. Both columns are equal width on desktop and stack at 1000px. More than 12 rows use the shared bounded selector, retaining access to long results without an equally long legend; shorter lists can expand in place. A full-width category-analysis card follows with the Revenue/Units switch, linked treemap and ranking, stacking its inner columns at 900px. Pareto, product trends and all affinity modes remain available.

Sales pairs colored order-channel rows with latest expense records. Four returned expenses appear per page, with a visible record range and previous/next controls; the two panels share the row height. DailyLedger keeps sorting, pagination, revenue, expenses and all channel columns, adding date badges and shaded revenue cells behind exact values. Its phone records retain every source column.

KitchenSpeed appears once in Products on the continuous page and remains in standalone Operations. Actual preparation bars and target markers use one scale that includes overruns. Each row retains category, order count, actual minutes, target or untracked state, and the numeric overrun. All categories and Above target filters use real counts; no overruns has its own honest empty state. Operations keeps real order-type, hourly and service information.

Materials: dashboard surfaces and KPI cards use 16px corners, a fine neutral edge and the shared dashboard two-part shadow. Overview and chapter KPIs use light chart-color tints and tabular Hanken figures; exact rows and accounting values retain JetBrains Mono. Time-series columns shade from the fourth chart color into primary, with softly rounded ends; area charts use fading fills. Hall, Delivery and Pickup share primary/fourth/third chart colors through orderChannelColors.ts across charts, legends and ledger rows. Ring sectors use roundedSector.ts with share-constrained gaps/corners. Dark floating tags use their own white text; other ring labels use the fixed light/dark-text pair, preserving contrast in both themes.

Media inventory: semantic Vue/CSS for controls, activity tiles, tables, ranked distributions and states; actual SVG paths for directly selectable ring sectors; existing ECharts SVG for time series, channel columns and category treemap; SVG for Pareto, staff and product-pair charts. No raster assets belong in this production dashboard. Existing fonts, palette tokens and DesignIcon remain shared.

Interaction: hover previews; click/tap pins an exact breakdown; row buttons provide keyboard equivalents. Category map, custom selector and ranking share selection. Series expose metric/area/column choices, comparison when backed by data, keyboard point navigation and optional zoom tools. Pareto retains every product with a keyboard-accessible exact-value selector. Product affinity retains ranked, chord and matrix modes. Staff comparisons use reported orders, revenue, hours and shifts, with exact values and explicit per-axis normalization; no invented performance scores.

Scatter plots orders against revenue using shaded rounded-square marks whose size changes with AOV; area is not a directly proportional AOV scale. Hover/focus previews a person's exact orders, revenue in UZS and AOV; click/tap or Enter/Space pins selection. Points have enlarged transparent hit areas, and the custom selector reaches all people including coincident points. Selection synchronizes with the staff leaderboard and primary Radar comparison. Moving away from a preview restores the pinned person's readout. Radar uses a 300-unit chart size, scales each measure to that measure's maximum across the reported team, and keeps axis labels at 11 CSS pixels as the chart responds. Color-linked person selectors and the initially open exact-value table identify both series.

Responsive layout: opening totals become two columns at 1250px. The first chart pair stacks at 1020px, while the payment/category pair stays two columns until 650px. Chapter KPI cards use two phone columns; Sales' first metric spans the row. Activity tiles use two phone columns. Card padding reduces from 20px to 16px; meaningful chart controls remain reachable with phone targets, while optional exploration tools open on demand. Distribution containers at 420px or narrower put a 220px ring above a two-column legend. Long translated labels wrap, and chart selection remains available to touch and keyboard users.

Truth: zero, unavailable, stale, loading and partial failure have different states. No live-looking mock data. Pair occurrences are not unique order counts. Chart share denominators and staff aggregation labels remain visible. Use shared UZS/count formatters and update en/ru/uz together. Preserve independent business-day today counts and existing canonical date construction.

Dataset scope: sold-product units use every returned Pareto product when all rows contain a valid quantity; older responses without a complete quantity set fall back to up to five positive-unit products from the available trend series. The product pie's denominator is the sum of that supplied dataset, never the overview's full-period units. Category unit shares use every returned category and their unit sum. Limiting the visible legend does not limit either pie's sectors or denominator. Pareto cumulative percentages prefer a positive reported period total, then complete monotonic reported cumulative shares, then complete reported shares, and finally the sum of displayed product revenues. Its scope copy distinguishes reported period revenue from chart revenue, and no 80% claim appears if the reported cumulative series does not reach that level. The staff non-cancellation summary is labeled as an average across staff, not a weighted order-wide rate.

Exports: Dashboard Export provides the complete backend product-performance report in XLSX, PDF and CSV for ADMIN/MANAGER using the applied business dates. The menu discloses the report's 07:00–03:00 scope and warns when an exact-time dashboard interval differs. The separate loaded Dashboard snapshot CSV retains the exact reporting window; missing values remain blank. Each backend format has its own pending state, duplicate guard and visible error. The full Product Performance report is integrated at /reports/product-performance and retains its own 14px report surfaces and metric rail.

Verification coverage: desktop/tablet/320–390px phones, Uzbek/Russian/English, both themes and all six palettes, hover/tap/keyboard, loading/error/empty/populated states, exact totals, filters, sorting, pagination, refresh and export. The September 10 finish verdict is PASS with all five material findings resolved: ring-label contrast, shared channel colors, expense-list height, actual kitchen overruns and readable phone radar labels. The existing implementation's request, business-day, role and data-truth boundaries remain in force.
