# Owner mobile app build (vite.owner.config.ts, `--mode owner`).
# Capacitor serves the bundle from capacitor://localhost / https://localhost,
# so every request must go to the absolute production API host.
VITE_API_HOST=https://pos.78.111.90.65.nip.io
